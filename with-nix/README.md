# With Non-flakes version

## Usage
```bash
nix-shell # Enter the nix-shell with cargo, rustc, rustfmt, and etc
nix-build # build the non-flakes version
nix-build ./treefmt.nix # Create formatter into result
```
